/**
 * X/Twitter reads its own tag. Same artwork as the Open Graph card — re-export
 * rather than duplicate, so the two never drift apart.
 */
export { default, alt, size, contentType } from "./opengraph-image";
